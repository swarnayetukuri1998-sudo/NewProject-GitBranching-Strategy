pipeline {
    agent any
    tools {
        maven 'MyMaven'
    }
    stages {
        stage('Code') {
            steps {
                git branch: 'develop', url: 'https://github.com/swarnayetukuri1998-sudo/NewProject-GitBranching-Strategy.git'
            }
        }
        stage('Build') {
            steps {
                sh 'mvn clean package'
            }
        }
        stage('Test') {
            steps {
                echo 'Running tests.........'
            }
        }
        stage('Deploy') {
            steps {
                deploy adapters: [tomcat9(alternativeDeploymentContext: '', credentialsId: 'e-commerce', path: '', url: 'http://3.108.249.25:8080')], contextPath: 'E-commerce', war: 'target/*.war'
            }
        }
    }
}
